---
// Compatibilidad: el diagnóstico (árbol) vivía aquí y se movió a /diagnostico.
// Esta ruta redirige de forma permanente para no romper enlaces antiguos.
export const prerender = false;
return Astro.redirect("/diagnostico", 301);
---
