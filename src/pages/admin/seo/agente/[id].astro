---
// /admin/seo/agente/:id migrado a hub.startidea.tech (2026-05-15).
// NOTA: los IDs del hub son distintos a los de SQLite — los antiguos
// agente/* no resuelven en el hub, redirige al dashboard SEO general.
export const prerender = false;
return Astro.redirect("https://hub.startidea.tech/startidea/admin/seo", 301);
---
