---
// /admin/seo ahora vive en hub.startidea.tech (StartIdea OS).
// Migrado 2026-05-15. Los datos GA4 + GSC + agentes IA fueron movidos
// de SQLite local a Postgres del hub. Esta página devuelve 301 permanente.
export const prerender = false;
return Astro.redirect("https://hub.startidea.tech/startidea/admin/seo", 301);
---
